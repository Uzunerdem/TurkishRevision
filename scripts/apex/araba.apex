string brand = 'Toyota';
system.debug(brand);

string model = 'Yaris';
system.debug(model);

string color = 'grey';
system.debug(color);

string myCar= brand +' '+ model+' ' + color;
system.debug(myCar);